# Local secrets — copy to .env and fill in (do not commit .env)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=ausie_retail_oltp

DUCKDB_PATH=duckdb_warehouse/warehouse.duckdb

# Free Gemini API key from Google AI Studio (for text-to-SQL)
GEMINI_API_KEY=
# Optional model override (default gemini-2.0-flash)
GEMINI_MODEL=gemini-2.0-flash

# Local secrets — copy to .env and fill in (do not commit .env)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=ausie_retail_oltp

DUCKDB_PATH=duckdb_warehouse/warehouse.duckdb
